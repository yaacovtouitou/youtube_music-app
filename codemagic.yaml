workflows:
  android-workflow:
    name: Android build workflow
    environment:
      android: "latest"
    scripts:
      - ./gradlew clean
      - ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/apk/release/app-release.apk
